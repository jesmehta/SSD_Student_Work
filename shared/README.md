# shared/

Reserved for resources genuinely shared across multiple galleries (typography,
CSS variables, common navigation, small components, utility scripts).

Empty for now. `ssd-creative-coding-2025-26/_shared/` holds that gallery's own
p5.js/p5.sound libraries and is intentionally left where it is rather than
pulled up here — nothing else needs them yet, and moving them prematurely
would risk breaking the one working gallery. Promote things here only once a
second gallery actually needs to share them.
