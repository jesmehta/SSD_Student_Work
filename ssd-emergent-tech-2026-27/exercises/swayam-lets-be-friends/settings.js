/* =========================================
   EVIL REPLICA
   v0.2.0 — Project Settings
   ========================================= */


const SETTINGS = {


    /* =====================================
       DIALOGUE TIMING
       ===================================== */

    // Delay after a complete dialogue line.
    dialogueDelay: 900,

    // Speed at which each character is typed.
    // Lower = faster.
    typingSpeed: 75,

    // Delay before the typing animation begins.
    typingStartDelay: 150,


    /* =====================================
       VISUAL TIMING
       ===================================== */

    // How quickly the dialogue fades.
    dialogueFadeDuration: 300,


    /* =====================================
       CAMERA
       ===================================== */

    // Text displayed while requesting access
    // to the user's camera.
    cameraPermissionMessage:
        "Give me a second...",

    // Delay before the camera appears after
    // permission has been granted.
    cameraAppearDelay: 500,


    /* =====================================
       INPUT
       ===================================== */

    // Allow pressing Enter to submit the name.
    allowEnterKey: true,

    // Space Bar skips the current typing animation.
    skipDialogueKey: " "

};