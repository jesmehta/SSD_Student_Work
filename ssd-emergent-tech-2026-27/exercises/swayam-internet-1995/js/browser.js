/* =========================================================
   NETSCAPE BROWSER ENGINE
   Phase 1 placeholder
   ========================================================= */

const BrowserEngine = {

    currentURL: "about:blank",

    init() {
        console.log("Netscape Browser Engine initialized.");
    }

};

document.addEventListener("DOMContentLoaded", () => {
    BrowserEngine.init();
});